receivers:
  nop:

processors:
  nop:

exporters:
  nop:

extensions:
  observiq_manager:
  observiq_manager/2:
    status_interval: 5s
    reconnect_interval: 30m
    endpoint: "ws://localhost"

service:
  extensions: [observiq_manager]
  pipelines:
    logs:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
