title: test-plugin
template: "{{.invalid"
version: 0.0.0
description: A test plugin with an invalid template
parameters:
- name: env
  type: string
  default: prod
  supported: ["prod","dev"]
  required: true
