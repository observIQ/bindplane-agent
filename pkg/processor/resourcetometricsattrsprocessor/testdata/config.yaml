receivers:
  nop:

processors:
  resourcetometricsattrs:
  resourcetometricsattrs/customname:
    operations:
      - from: "some.resource.level.attr"
        to: "some.metricdatapoint.level.attr"
      - from: "another.resource.attr"
        to: "another.datapoint.attr"

exporters:
  nop:

service:
  pipelines:
    metrics:
      receivers: [nop]
      processors: [resourcetometricsattrs]
      exporters: [nop]